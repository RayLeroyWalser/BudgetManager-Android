<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">
    <ImageButton
        android:id="@+id/search_institution_imageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:background="@null"
        android:src="@android:drawable/ic_menu_search"/>
    <EditText
        android:id="@+id/search_institution_editText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/search_institution_imageButton"
        android:singleLine="true"
        android:hint="@string/hint_institution" />
    <ListView
        android:id="@+id/search_institution_listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_institution_imageButton"
        android:layout_marginTop="10dp"/>
</RelativeLayout>